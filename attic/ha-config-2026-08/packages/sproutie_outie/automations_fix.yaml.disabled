  # Automation: Clear Selection on Mode Change
  - id: sproutie_clear_selection_on_mode_change
    alias: "Clear Selection on Mode Change"
    description: "Clears slot selection when switching between Seeding, Care, and Harvest modes"
    trigger:
      - platform: state
        entity_id: input_select.op_mode
    action:
      - service: script.sproutie_clear_selection
    mode: single
